/**
 * Windowed Workspace Host half. It registers nothing on the Host; its browser half is part of this package's `./client` bundle.
 * Loaded by the `ui-workspace-windowed` row of `cordis.patch.yml`.
 * @module @psychiiii/dsh-three-window/workspace
 */

export * from '@psychiiii/dsh-three-window-workspace'
