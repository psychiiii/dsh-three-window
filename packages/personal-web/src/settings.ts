/**
 * Review settings Host half. It registers nothing on the Host; its browser half is part of this package's `./client` bundle.
 * Loaded by the `personal-settings` row of `cordis.patch.yml`.
 * @module @psychiiii/dsh-three-window/settings
 */

export * from '@psychiiii/dsh-three-window-settings'
