/**
 * Three-window workbench Host half: validates the window rows and injects them into the served page.
 * Loaded by the `ui-workbench` row of `cordis.patch.yml`.
 * @module @psychiiii/dsh-three-window/workbench
 */

export * from '@psychiiii/dsh-three-window-workbench'
